import { Request, Response } from "express";
import { userModel } from "../Models/user.model.js"
import { getGenAI } from "../Gemini Ai/gemini.js";

/**
 * @name adminHelper
 * @desc this is a assistant , who helps admin 
 * @acess Private
 * @route POST/api/helper
 */

export const adminHelper = async (req: Request, res: Response) => {
    try {

    }
}